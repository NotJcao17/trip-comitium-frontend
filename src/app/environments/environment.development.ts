// Se usa solo con `ng serve` / build de desarrollo (ver fileReplacements en angular.json).
// La build de producción sigue apuntando al backend desplegado.
export const environment = {
    production: false,
    apiUrl: 'http://localhost:3000/api'
};
